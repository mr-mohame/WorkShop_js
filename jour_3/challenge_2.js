//Affichez uniquement les nombres pairs de 1 à 20 en utilisant une boucle while .
const prompt = require('prompt-sync')();
let compteur = 2;
while(compteur <= 20){
    console.log(`nomber ${compteur}`);
    compteur = compteur + 2;
   
}